import Picture from '../Picture/Picture.jsx'

function Grid() {
    return(
        <div>
            {/* will have to .map to go through all of the data in the array. */}
            <Picture />
        </div>
    )
}

export default Grid;